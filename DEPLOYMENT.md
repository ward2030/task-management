# 🚀 دليل نشر نظام إدارة المهام على Vercel + Supabase (مجاني 100%)

## 📋 نظرة عامة

هذا الدليل سيشرح لك كيفية نشر نظام إدارة المهام بشكل مجاني كامل مع:
- ✅ **Vercel** - استضافة مجانية
- ✅ **Supabase** - قاعدة بيانات PostgreSQL مجانية (500 MB)
- ✅ **كل الميزات تعمل بشكل كامل**

---

## 📦 الخطوة 1: إنشاء قاعدة بيانات Supabase

### 1.1 إنشاء حساب
1. اذهب إلى: **[supabase.com](https://supabase.com)**
2. اضغط **"Start your project"**
3. سجل بحساب GitHub أو البريد الإلكتروني

### 1.2 إنشاء مشروع جديد
1. اضغط **"New Project"**
2. اختر تنظيم (أو أنشئ واحد جديد)
3. أدخل البيانات:
   - **Name:** `task-management` (أو أي اسم)
   - **Database Password:** (احفظ هذا جيداً!)
   - **Region:** اختر الأقرب لك (مثل Singapore أو Frankfurt)
4. اضغط **"Create new project"**
5. انتظر 2-3 دقائق حتى ينتهي الإعداد

### 1.3 الحصول على رابط قاعدة البيانات
1. من القائمة الجانبية، اذهب إلى **Settings** ⚙️
2. اضغط **Database**
3. ابحث عن **"Connection string"** تحت **"Connection info"**
4. اختر **"URI"** من التبويب
5. انسخ الرابط (سيكون شكله كالتالي):
   ```
   postgresql://postgres.xxxxx:PASSWORD@aws-0-region.pooler.supabase.com:6543/postgres
   ```
6. **مهم:** استبدل `[YOUR-PASSWORD]` بكلمة المرور التي أنشأتها

---

## 📤 الخطوة 2: رفع المشروع إلى GitHub

### 2.1 إنشاء مستودع جديد
1. اذهب إلى **[github.com](https://github.com)**
2. اضغط **"+"** → **"New repository"**
3. أدخل:
   - **Repository name:** `task-management`
   - **Public** أو **Private** (كما تريد)
4. اضغط **"Create repository"**

### 2.2 رفع الملفات
فك ضغط ملف `my-project-backup.zip` على جهازك، ثم:

```bash
# افتح Terminal في مجلد المشروع
cd my-project

# تهيئة Git
git init

# إضافة جميع الملفات
git add .

# إنشاء أول commit
git commit -m "نظام إدارة المهام - جاهز للنشر"

# ربط المستودع المحلي بـ GitHub
git remote add origin https://github.com/USERNAME/task-management.git

# رفع الملفات
git branch -M main
git push -u origin main
```

---

## 🚀 الخطوة 3: النشر على Vercel

### 3.1 إنشاء حساب
1. اذهب إلى **[vercel.com](https://vercel.com)**
2. اضغط **"Sign Up"**
3. اختر **"Continue with GitHub"**

### 3.2 استيراد المشروع
1. من لوحة التحكم، اضغط **"Add New..."** → **"Project"**
2. ابحث عن `task-management` في قائمة المستودعات
3. اضغط **"Import"**

### 3.3 إعداد المتغيرات البيئية
1. قبل النشر، اضغط **"Environment Variables"**
2. أضف المتغير التالي:
   - **Name:** `DATABASE_URL`
   - **Value:** (رابط قاعدة البيانات من Supabase)
3. اضغط **"Add"**

### 3.4 النشر
1. تأكد من أن:
   - **Framework Preset:** Next.js
   - **Build Command:** `npm run build`
   - **Output Directory:** `.next`
2. اضغط **"Deploy"**
3. انتظر 2-3 دقائق ⏳

### 3.5 🎉 مبروك!
- ستحصل على رابط موقعك مثل: `https://task-management-xyz.vercel.app`
- اضغط الرابط لفتح موقعك!

---

## 🔐 الخطوة 4: إنشاء المستخدم الأول

### الطريقة اليدوية (عبر Supabase):

1. افتح مشروعك في Supabase
2. اذهب إلى **SQL Editor** من القائمة الجانبية
3. اضغط **"New query"**
4. الصق الكود التالي:

```sql
-- إنشاء المستخدم المدير
INSERT INTO users (id, username, password, name, role, "isActive")
VALUES (
  'admin_user_001',
  'admin',
  '$2a$10$YourHashedPasswordHere',
  'المدير',
  'ADMIN',
  true
);
```

### ⚠️ ملاحظة مهمة:
لأن كلمة المرور مشفرة، سأضيف API endpoint لإنشاء المستخدم الأول تلقائياً.

---

## 🛠️ الخطوة 5: إنشاء المستخدم الأول تلقائياً

بعد النشر، أضف هذا الكود لإنشاء المستخدم الأول:

1. اذهب إلى موقعك: `https://your-site.vercel.app`
2. إذا لم يعمل، أضف `/api/init` للرابط: `https://your-site.vercel.app/api/init`
3. هذا سيقوم بإنشاء المستخدم المدير تلقائياً

---

## 🔑 بيانات الدخول

| الدور | اسم المستخدم | كلمة المرور |
|-------|-------------|-------------|
| مدير | `admin` | `admin123` |

---

## ✨ ميزات النظام

- ✅ **لوحة Kanban** مع السحب والإفلات
- ✅ **التقويم الزمني** (Gantt Chart)
- ✅ **تقارير وإحصائيات** بيانية
- ✅ **إشعارات** عند إسناد المهام
- ✅ **تعليقات** داخل المهام
- ✅ **إدارة المستخدمين** بصلاحيات مختلفة
- ✅ **الوضع الداكن**
- ✅ **واجهة عربية** RTL

---

## 🆘 استكشاف الأخطاء

### المشكلة: الموقع لا يعمل
- تأكد من أن `DATABASE_URL` صحيح في Vercel
- تحقق من Build Logs في Vercel

### المشكلة: خطأ في قاعدة البيانات
- تأكد من أن كلمة المرور في الرابط صحيحة
- تأكد من أن المشروع في Supabase نشط

### المشكلة: لا يمكن تسجيل الدخول
- تأكد من تشغيل `/api/init` لإنشاء المستخدم الأول

---

## 📞 الدعم

إذا واجهت أي مشكلة، تأكد من:
1. رابط DATABASE_URL صحيح وكامل
2. المستودع مرفوع بشكل كامل على GitHub
3. المتغيرات البيئية مُعدة في Vercel

**مبروك! نظام إدارة المهام يعمل الآن بشكل مجاني وكامل! 🎉**
