# نظام إدارة المهام

نظام احترافي لإدارة المهام وإسنادها للموظفين مع واجهة كانبان تفاعلية.

## ✨ المميزات

- 📋 **إدارة المهام**: إنشاء وتعديل وحذف المهام
- 📊 **لوحة كانبان**: سحب وإفلات المهام بين الأعمدة
- 👥 **إدارة المستخدمين**: ثلاثة أدوار (مدير، منسق، موظف)
- 💬 **التعليقات**: إضافة تعليقات على المهام
- 🔔 **الإشعارات**: إشعارات فورية عند إسناد المهام
- 📈 **التقارير**: إحصائيات ورسوم بيانية
- 🌙 **الوضع الداكن**: دعم كامل للوضع الداكن

## 🚀 النشر على Vercel (مجاني)

### الخطوة 1: إنشاء حساب
1. اذهب إلى [vercel.com](https://vercel.com)
2. سجل بحساب GitHub

### الخطوة 2: رفع المشروع على GitHub
```bash
# إنشاء مستودع جديد على GitHub ثم:
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/USERNAME/REPO_NAME.git
git push -u origin main
```

### الخطوة 3: النشر على Vercel
1. من لوحة تحكم Vercel، اضغط **"New Project"**
2. اختر المستودع من GitHub
3. اضغط **"Deploy"**

### الخطوة 4: إضافة قاعدة بيانات
1. من لوحة Vercel، اذهب إلى **Storage**
2. أنشئ قاعدة بيانات **Postgres** مجانية
3. انسخ `DATABASE_URL` وأضفها في **Environment Variables**

## 🗄️ قاعدة البيانات

### للنشر (PostgreSQL)
```env
DATABASE_URL="postgresql://user:password@host:5432/dbname"
```

### للتطوير المحلي (SQLite)
```env
DATABASE_URL="file:./dev.db"
```

## 👤 بيانات الدخول الافتراضية

```
اسم المستخدم: admin
كلمة المرور: admin123
```

## 🛠️ التثبيت المحلي

```bash
# تثبيت الحزم
bun install

# إنشاء قاعدة البيانات
bun run db:push

# تشغيل المشروع
bun run dev
```

## 📁 هيكل المشروع

```
src/
├── app/
│   ├── api/          # API endpoints
│   ├── page.tsx      # الصفحة الرئيسية
│   └── layout.tsx    # الهيكل العام
├── components/
│   ├── layout/       # القائمة والهيدر
│   ├── tasks/        # صفحات المهام
│   └── users/        # صفحة المستخدمين
├── store/            # إدارة الحالة
└── lib/              # المساعدات
```

## 📜 الرخصة

MIT License
